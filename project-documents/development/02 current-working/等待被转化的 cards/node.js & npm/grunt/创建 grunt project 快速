进入项目文件夹

从现有 grunt 项目复制 package.json
  npm install

  * grunt 使用 npm 做 package management
  * package.json 告诉 npm 安装 grunt 和需要用到的 grunt pluging
  * 编辑 package.json，然后用 npm install 安装 grunt 和所有 pluging


从现有 grunt 项目复制 Gruntfile.js

  * 在 package.json 里已经重新定义了需要用到的 grunt pluging
  * 在 Gruntfile.js 用这些 pluging 重新定义任务


用 grunt CLI 运行任务
  grunt <任务名>
