module.exports = function(grunt) {
    'use strict';

    return {
        shell: {
/*
            options: {
                stdout: true,
                failOnError: true,
                execOptions: {
                    maxBuffer: 500*1024
                }
            },

            serve: {
                command: 'bundle exec wagon serve > /dev/null 2>&1 &'
            }
*/
        }
    };
};
