module.exports = {
	replace: {
		/*
		css: {
			options: {
				// Replace rails/sass tags with relative path urls
				usePrefix: false,
				patterns:  [{
					match: 'image-url',
					replacement: 'url'
				}]
			},

			files: [{
				expand:  true,
				flatten: true,
				src:     '<%= yeoman.build %>/stylesheets/*.css',
				dest:    '<%= yeoman.build %>/stylesheets'
			}]
		}
		*/
	}
};
