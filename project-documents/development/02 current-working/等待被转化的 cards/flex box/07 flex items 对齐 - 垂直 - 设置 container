设置 container 的 align-items，-webkit-align-items
  https://codepen.io/HarryWang74/pen/OmpBOK?editors=1100#0
  
  align-items
  -webkit-align-items

  stretch:
    默认值，children 与 container 等高

  flex-start
    children 高度按实际内容，对齐 container flex axis 的起始位置

  flex-end:
    children 高度按实际内容，对齐 container flex axis 的终止位置

  center:
    children 高度按实际内容，对齐 container flex axis 的中部

  baseline:
    children 高度按实际内容，对齐 container flex axis 的基线，类似 flex-start
